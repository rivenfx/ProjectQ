import { ConfirmValidatorDirective } from './confirm-validator.directive';

describe('ConfirmValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
